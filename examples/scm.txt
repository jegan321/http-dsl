default host http://localhost:8080/scm

default header Authorization = Basic {{ base64('trivalence_admin:admin')}}
default header X-Facility-ID = 1

print should search for a facility item
    GET /api/v1/facility-items/search

    set facilityItemId = {{ response.body.results[0].facilityItemId }}

    print facilityItemId={{ facilityItemId }}
print

print should get facility item details
    GET /api/v1/facility-items/{{ facilityItemId }}

    set pricingId = {{ response.body.pricings[0].id }}

    print pricingId={{pricingId}}
print

print should add to cart
    POST /api/v1/purchase-order-request-drafts/line-items
    [
        {
            "facilityVendorItemPricingId": {{ pricingId }},
            "quantity": 1,
            "purchaseOrderTypeCode": "STANDARD",
            "shippingTypeCode": "BEST",
            "price": 12.34
        }
    ]
    print {{response}}
print