GET /items

set items = {{ response.body.results }}

if {{ !items.length }}
    exit No items returned from API!
end

if {{ items.length }}

    for item in {{ items }}
        POST /cart
        {
            "itemId": "{{ item.id }}"
        }

    end 

end

